"""Per-capability MCP tool modules.

Each module exposes ``register(mcp, registry)``. To add a capability, create a
new module here and add it to ``CAPABILITY_MODULES`` in
``flight_assistant.mcp_server.server``.
"""
